/**
 *  KYC CDD : Review - List
 */

'use client'

import {
  type ReactElement,
  useEffect
} from 'react';
import { ListView } from '../_component/list-view';

const Page = (): ReactElement => {
  useEffect(() => { }, []);

  return (
    <ListView />
  );
}

export default Page;